# Will run by Raspberry Pi 4
# Temporarily store sensor_id, temperature pairs in dictionaries



def insert_data(temperature, sensor_id):
    # Write a function that...
    # Compare sensor_id, if found matching id and insert temperature to the respective id
    # Else sensor_id not found in dictionary, add new sensor id and insert temperature
    return

