# Will run by Raspberry Pi 4
# Store sensor_id and sensor data into data_manager.csv
# Historical data is not required, each sensor_id will occupy one row, and subsequent columns are for various sensor data

sensor_id = []

def write_data(sensor_id, sensor_data1, sensor_data2):
    # Write a function that...
    # Compare sensor_id, if found matching id and insert sensor data to the respective id
    # Else sensor_id not found in list, add new sensor id and insert sensor data
    return

